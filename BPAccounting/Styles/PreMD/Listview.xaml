<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:BPAccounting">

    <Style
        TargetType="TextBlock"
        x:Key="ListViewHeader"
        >
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="Foreground" Value="{StaticResource NormalTextBrush}"/>
    </Style>

    <Style
        TargetType="ListViewItem"
        x:Key="MaterialListViewItem"
        >
        <Setter Property="Foreground" Value="{StaticResource NormalTextBrush}"/>
        <Setter Property="Padding" Value="0 12"/>
    </Style>
    
    <Style
        TargetType="ListView"
        x:Key="MaterialListView"
        >
        <Setter Property="BorderThickness" Value="1 0"/>
        <Setter Property="BorderBrush" Value="{StaticResource DividerBrush}"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="Padding" Value="24"/>
        <Setter Property="ItemContainerStyle" Value="{StaticResource MaterialListViewItem}"/>
    </Style>

</ResourceDictionary>